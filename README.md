# Reliable Bridge Transport Layer: transmit data over the net, reliably

### Feature flags

* `serde`: allow to serialize ConnectInfo and deserialize into ConnectInfoParsed
* `default-export`: Create RBTLListener with the macro, with this crate's defaults